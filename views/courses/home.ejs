<%layout("./layouts/boilerplate.ejs")-%>
<!-- Administrative Actions -->
<div class="row col-6 mt-3">
  <div>
    <h4>Administrative Actions</h4>
    <a href="/courses/new" class="btn btn-warning mb-1">Create new course</a>
    <div class="mt-1">
      <a href="#" class="btn btn-secondary mb-1">My courses</a>
      <a href="#" class="btn btn-success mb-1">Progress</a>
    </div>
  </div>
</div>
<hr />
<!-- <form action="/courses/new" method="get">Create Course</form> -->
<div class="row cols-8 flex-wrap gap-4 mt-3">
  <% for(course of courses){%>
  <div class="card ml-3" style="width: 16rem">
    <div class="card-body">
      <h5 class="card-title"><%=course.CourseName%></h5>
      <!-- <p class="card-text">
        With supporting text below as a natural lead-in to additional content.
      </p> -->
      <div>
        <p>Educator Name</p>
        <p>no.of Enrollements</p>
      </div>
      <div>
        <a href="/courses/<%=course.id%>/chapters" class="btn btn-secondary"
          >view Course</a
        >
        <form action="/courses/<%=course.id%>/enroll" method="post">
          <input type="hidden" name="_csrf" value="<%=csrfToken%>" />
          <button class="btn btn-primary">Enroll</button>
        </form>
      </div>
    </div>
  </div>
  <%}%>
</div>
